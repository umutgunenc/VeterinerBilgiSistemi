@model KanTahlilleriniGetirViewModel

@section AdminMenu {
    @await Html.PartialAsync("_AdminMenu")
}

<form asp-action="KanTahliliSil" asp-controller="Admin" method="post">
    <div class="form-row">
        <div class="col">
            <select asp-for="KanDegerleriId" class="form-control">
                <option selected disabled>Silinecek Kan Tahlilini Seçiniz</option>
                @foreach (var item in Model.KanTahlilleriListesi)
                {
                    <option value="@item.Value">@item.Text</option>
                }
            </select>
        </div>
        <button type="submit" class="btn btn-dark rounded mb-2">Sil</button>
    </div>
    <div class="col">
        <span class="row font-weight-bold text-danger bg-light text-danger rounded" asp-validation-for="KanDegerleriId">
        </span>
    </div>
</form>

@if (TempData["KanTahliliSilindi"] != null)
{
    <div class="alert alert-success" id="KanTahliliSilindi">@TempData["KanTahliliSilindi"]</div>

}

@section scripts{
    <script>
        setTimeout(function () {
            var successMessage = document.getElementById("KanTahliliSilindi");
            successMessage.remove();
        }, 2500);
    </script>
}